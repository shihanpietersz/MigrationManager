# ===========================================
# DrMigrate Azure Sync - Web Configuration
# ===========================================
# Copy this file to .env.local

# API URL - full path to the backend API (include /api/v1)
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# ===========================================
# NOTE: Azure credentials are configured via the
# Settings page in the application UI, not here.
# ===========================================

