# ===========================================
# DrMigrate Azure Sync - API Configuration
# ===========================================
# Copy this file to .env

# Database (SQLite) - REQUIRED
DATABASE_URL=file:./data/drmigrate.db

# Server Configuration (optional - these are the defaults)
PORT=4000
HOST=0.0.0.0
NODE_ENV=development

# Frontend URL for CORS (optional - defaults to localhost:3000,3001)
FRONTEND_URL=http://localhost:3000

# ===========================================
# NOTE: Azure credentials are NOT configured here!
# Configure Azure via the Settings page in the UI:
#   http://localhost:3000/settings
# ===========================================

